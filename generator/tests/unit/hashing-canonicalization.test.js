"use strict";
/**
 * T055 Hash canonicalization edge test (scaffold)
 */
describe('hash canonicalization (scaffold)', () => {
    test('whitespace & float normalization stable', async () => {
        // const a = canonicalSerialize({ text: 'Hello   World', value: 1.234567 });
        // const b = canonicalSerialize({ value: 1.2345671, text: 'Hello World' });
        // expect(hashContent(a)).toEqual(hashContent(b));
        expect(false).toBe(true);
    });
});
//# sourceMappingURL=hashing-canonicalization.test.js.map